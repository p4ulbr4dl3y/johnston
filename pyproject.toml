[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "johnston"
version = "0.5.0"
description = "Textual AI Johnston Application"
requires-python = ">=3.10, <3.14"
dependencies = [
    "textual>=8.0.0",
    "openai>=1.0.0",
    "httpx>=0.25.0",
    "rich",
    "pygments",
    "markdown-it-py",
    "pillow>=12.3.0",
    "markitdown>=0.1.0",
]

[project.scripts]
johnston = "cli:main"

[tool.hatch.build]
exclude = [
    ".github",
    ".johnston",
    ".ruff_cache",
    ".serena",
    "AGENTS.md",
    "dist",
    "install.ps1",
    "install.sh",
    "tests",
    "uv.lock",
]

[tool.hatch.build.targets.wheel]
only-include = [
    "app.py",
    "app.tcss",
    "cli.py",
    "core",
    "tools",
    "widgets",
]

[dependency-groups]
dev = [
    "coverage>=7.0.0",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "ruff>=0.15.22",
]

[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
python_files = ["test_*.py"]
asyncio_mode = "auto"
filterwarnings = [
    "ignore::DeprecationWarning",
]

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"app.py" = ["E402"]

[tool.deptry.package_module_name_map]
"markdown-it-py" = ["markdown_it"]
pillow = ["PIL"]

"""Pure presentation helpers for UI widgets.

Modules in this package have NO ``from core...`` or ``from tools...`` imports.
Anything that drags in core/tools lives in the UI layer (e.g. widget-local diff builders).
"""

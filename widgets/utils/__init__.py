"""Pure presentation helpers for UI widgets.

Modules in this package have NO ``from core...`` or ``from tools...`` imports.
Anything that drags in core/tools lives in the UI layer (e.g. the registry
wrappers in ``widgets.tool_helpers`` / widget-local diff builders).
"""

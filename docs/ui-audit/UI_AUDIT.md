# Аудит UI Johnston v0.28.0 — соответствие современным стандартам

Дата: 2026-09-01, обновлено 2026-09-02 · ветка `arena/01a05e32-johnston` · коммит `0eac100` → `2229f07`
Объект: Textual-TUI (Python 3.10+, textual 8.2.8), стили `app.tcss` (1296 строк), 21 тема, ~60 виджетов/экранов.

---

## 0. Статус: спринты 1-3 выполнены (обновление от 2026-09-02)

P0-1…P0-4 закрыты. Замеры — той же методикой (§1), на 17–18 сценариях снимков **без модальных оверлеев**
(текст под затемнением и подсветка синтаксиса исключены), 5 279 → 5 254 текстовые ячейки и 1 432 ячейки границ.
Заодно уточнён парсер SVG-снимков (фон брался со строки выше) — цифры «было» пересчитаны заново, так что пара сопоставима.

| Метрика | Было | Стало |
|---|---|---|
| Текст ниже WCAG AA 4.5:1 | **39.1%** | **3.1%** |
| Ячейки границ ниже 3:1 (1.4.11) | **82.8%** | **0.0%** |
| Токен `fg-muted` ≥4.5:1 (из 20 тем) | 2 | **20** |
| Токен `border` ≥3:1 (из 20 тем) | 0 | **20** |
| Футер: режим/ветка на 80 колонках | потеряны | **сохранены** |
| Оверлей модалки | дефолт Textual `rgba(0,0,0,0.45)` | **из темы** (zinc `rgba(0,0,0,0.62)`, github-light `rgba(218,220,225,0.85)`) |
| Выделение строки в модалке | только `bold` | **инверсия `$bg-inverted`/`$fg-inverted` + bold** |
| Граница модалки над затемнением | 1.86:1 (тёмные) / 2.46:1 (светлые) | **4.04:1 / 3.04:1** |

Что сделано в коде:

- `core/domain/policies/theme_contrast.py` — политика WCAG (`contrast_ratio`, `TEXT_CONTRAST_AA`, `UI_CONTRAST_AA`, `meets_on_all`).
- `core/domain/defaults/themes.json` — `muted` поднят до ≥4.5:1 и `border` до ≥3:1 во всех 20 палитрах, добавлен `bg-overlay` в каждую тему; в 4 светлых палитрах `border` дополнительно затемнён (`#8b9095` → `#7c8085` в github-light), чтобы край диалога держал 3:1 поверх композиции с затемнением.
- `core/infrastructure/platform/terminal_theme.py` — `fg_muted`/`border` считаются контраст-безопасно (`_contrast_safe_lerp`), больше не уходят ниже порога при переносе темы в терминал.
- `core/domain/policies/theme_contrast.py` — ещё `composite()`: модалка рисует `bg-overlay` поверх фона, поэтому границы панелей проверяются не по «сырому» фону, а по композиции (иначе в светлых темах край диалога падал до 2.46:1).
- `app.tcss` — три правила `-highlighted` (модалка, `#command-suggestions`, `#diff-file-list`) теперь инверсия; `ModalScreen` использует существующий `$bg-overlay`.
- `widgets/utils/row_format.py` — `fit_row()`/`compose_rows()`; футер, шапка сабагента и рамка стрима собирают строки по бюджету ширины и схлопываются многоточием, а не клипом. Rich-`Table.grid(expand=True)` удалён из `widgets/status_footer.py` и `widgets/presentation/widgets/subagent_footer.py`.
- Тесты: `tests/ui/test_ui_theme_contrast.py` (все темы × токены), `tests/ui/test_ui_status_footer_width.py` (40…200 колонок), `tests/ui/test_ui_modal_a11y.py` (оверлей + маркер выделения).

Снимки «после» — `02-chat-140-after.png`, `11-chat-80-after.png`, `27-modal-highlight-after.png`, `30-github-light-after.png`.

**Осталось ниже AA (3.1% ячеек, ~1% без учёта глифов рамок):** описание команды в popup-command-suggestions — Rich-разметка `[dim]`, 3.44:1 (не токен темы), и цвета Pygments в diff. Это полировка спринта 2, как и P1-5.

**Открытый вопрос:** границы панелей теперь ≥3:1 — это требование WCAG 1.4.11, но визуально рамки стали заметно плотнее. Если хочется мягче, можно оставить 3:1 только на модалках/панелях кода, а декоративные разделители вернуть к ~1.6:1 (нужен отдельный токен `border-subtle`).

## 0.2 Спринт 2 выполнен (обновление от 2026-09-01)

**P1-5. Статусные точки берутся из активной темы.** `widgets/utils/theme_colors.py` читает
`accent-warning/error/success` из текущей темы и, если акцент не дотягивает до 4.5:1 на фоне приложения
*или* поверхности, минимально сдвигает его к белому/чёрному. Константы `COLOR_STATUS_*` остались фолбэком
для headless-рендера. Тема, чей акцент уже читаем, используется как есть.

| Тема | running было → стало | error было → стало | success было → стало |
|---|---|---|---|
| github-light (фон #ffffff) | 2.30:1 → **4.87:1** | 4.00:1 → **5.36:1** | 2.86:1 → **5.08:1** |
| zinc-light (#ffffff) | 2.30:1 → **5.02:1** | 4.00:1 → **4.99:1** | 2.86:1 → **5.02:1** |
| catppuccin-latte (#eff1f5) | 2.04:1 → **4.92:1** | 3.54:1 → **4.88:1** | 2.53:1 → **4.89:1** |
| zinc (#18181b) | 7.69:1 (без изменений) | 4.43:1 → **5.35:1** | 6.19:1 (без изменений) |

Побочный эффект: теперь точка использует акцент самой темы — в tokyo-night это `#9ece6a`, в github-light
`#1a7f37`, вместо одинакового везде «zinc-зелёного».

**P1-6. Списки показывают, что они скроллятся.** Скроллбар в 1 колонку включён для списков в модалках и для
popup команд (`scrollbar-size-vertical: 1`, цвет `$fg-muted` на `$bg-surface`); проза и чат остались чистыми.
Popup команд вырос с 5 до 9 строк. `BaseSelectionScreen` добавляет к подсказке `• позиция/всего`, пока список
реально скроллится (для списка тем — `enter: select • esc: cancel • 1/21`), и убирает счётчик, когда поиск
сузил список до экрана.

**P1-8. Выделение мышью там, где это контент.** Включено для вывода инструментов (`ToolCallWidget`) и для
содержимого diff-просмотрщика (ветка screen → контейнер → scroll box → содержимое); хром — футеры, шапка/подвал
diff, поле поиска и список файлов — остался невыделяемым, чтобы клик по файлу не превращался в выделение текста.
Проза сообщений и раньше выделялась.

**P1-9. Обратная связь во время генерации и пустые состояния.** Пока идёт ответ, в футере вместо счётчиков
сервисов показывается `esc: interrupt • 12.4s`; счётчик живёт на перерисовке спиннера, поэтому тикает без
полного пересчёта статуса. `/subagents` и `/shell` больше не отвечают тостом: экран открывается всегда, а
`BaseTasksListScreen` при пустом списке рисует состояние «No subagents yet.» с подсказкой, что делать
(это же покрывает случай «убили последнюю задачу» — раньше экран просто закрывался).

**P1-7 (max-width текста) и P2 остаются в спринте 3.**

Новые тесты: `tests/ui/test_ui_status_colors.py` (44), `test_ui_list_scroll_feedback.py` (4),
`test_ui_selection_policy.py` (5), `test_ui_generation_feedback.py` (5). Прогон `-m "not slow"`:
**3910 passed**; `-m slow`: 67 passed. Единственный стабильно падающий
`tests/core/test_provider_manager.py::test_active_provider_field_is_ignored` и
`tests/ui/test_app.py::test_chat_app_flow` падают и на чистом `main`.

Снимки: `40-generating-footer.png` (esc: interrupt + таймер), `41-subagents-empty.png` (пустое состояние).

## 0.3 Спринт 3 выполнен (обновление от 2026-09-02)

**P1-7. Ширина строки текста на широких терминалах.** Абзац больше не растягивается на 180 символов:
`ProseMarkdown` ограничивает связный текст 110 знакоместами, когда виджет шире этого (`PROSE_MAX_WIDTH`),
а код и таблицы остаются во всю ширину — переносить код хуже, чем читать длинную строку. Ограничение
снимается на узких терминалах и пересчитывается на ресайз. Снимок `43-prose-max-width.png`: проза —
~110 колонок, блок кода под ней — все 196.

**P2-10. Экран приветствия учит, а не только красится.** Под логотипом — строка состояния
`v0.28.0 • Claude Sonnet 4 5 20250929 via Anthropic`, а если провайдер не настроен —
`v0.28.0 • no provider configured — /providers`; на узком терминале строка обрезается многоточием.
Ниже — блок подсказок (`/help`, `@file`, `ctrl+o`, `ctrl+c`), который исчезает вместе с логотипом
на <52 колонках. Снимок `42-welcome-screen.png`.

**P2-11. Esc называется одинаково.** Три константы в `screens/constants.py`: `close` — для
просмотрщиков и селекторов, `cancel` — когда экран выбрасывает введённое или отменяет действие
с побочным эффектом (api-ключи, rename, fork, rewind, ask-user), `back` — для экранов, накрывающих
другой. Темы говорили `cancel` при том же поведении, что и `close` у остальных селекторов — поправлено.
Заодно `space: toggle` в `/skills`, `/mcp` и `/providers` стал объявленным биндингом (до этого
обрабатывался в `_on_key` и был невидим для футера и `/help`); `space_actions` + `check_action`
в `ModalSearchNavMixin` оставляют пробел пробелом, когда он набирается в поле поиска.

**P2-12. Мелочи markdown.**
- Списки задач больше не `• [x] …`: маркер списка становится чекбоксом (☑ / ☐ той же ширины, что `• `),
  а `[x] ` вырезается из `Content` срезом — так внутренние стили (bold, code, ссылки) не теряются.
- Цитата получила вертикальную черту (`MarkdownBlockQuote`: `border-left: solid $border-subtle`)
  вместо одного отступа.
- В `/models` рядом с «очеловеченным» именем показывается исходный id, когда имя о нём не говорит:
  `Claude Sonnet 4.5` может быть и `claude-sonnet-4.5`, и `claude-sonnet-4-5` — теперь это видно
  (и это же значение ищется, фильтр и так матчил кортеж элемента).

**P2-13. Diff: в сайдбаре путь, а не только basename.** Строки строятся от cwd (`tests/ui/test_foo.py`
и `widgets/ui/test_foo.py` больше не выглядят одинаково), а длинные пути обрезаются **по середине**
(`middle_ellipsize` в `row_format.py`) — хвост у пути информативнее головы.

**P2-14. `!important`: 41 → 6.** Почти все были глобальным выключателем скроллбаров
(`* { scrollbar-size: 0 0 !important }` + `ScrollBar { display: none !important }`), из-за которого
каждое следующее правило, желающее скроллбар обратно, тоже вынуждено было быть `!important`.
Выключатель стал `Widget { scrollbar-size: 0 0 }` — `Widget` матчит все узлы (включая `Screen`) и
выигрывает у собственного `Widget { scrollbar-size-vertical: 2 }` из Textual порядком каскада, а не
силой; более специфичные правила (в т.ч. скроллбар списков из P1-6) перекрывают его без `!important`.
Оставшиеся 6 — цвет каретки и выделения: Textual стилизует их по теме (`TextArea:dark .text-area--cursor`),
а Johnston принудительно ставит true-color каретку; это задокументировано в начале `app.tcss`.

Новые тесты: `tests/ui/test_ui_prose_width.py` (4), `test_ui_welcome_screen.py` (6),
`test_ui_hint_consistency.py` (5), `test_ui_markdown_nits.py` (11), `test_ui_diff_sidebar.py` (6),
`test_ui_tcss_important.py` (4). Прогон `-m "not slow"`: **3947 passed**; единственный стабильно
падающий `tests/core/test_provider_manager.py::test_active_provider_field_is_ignored` падает и на
чистом `main`.

Снимки: `42-welcome-screen.png` (версия, состояние подключения, подсказки),
`43-prose-max-width.png` (200 колонок: проза 110, код во всю ширину).

---

## 1. Методология

Оценка не «на глаз», а по воспроизводимой процедуре:

1. **Статический анализ** — `app.tcss`, `core/domain/defaults/themes.json`, `widgets/**`. Метрики: хардкод-цвета, `!important`, брейкпоинты, `BINDINGS`, `ALLOW_SELECT`, работа с шириной строк.
2. **Динамический прогон** — headless-запуск приложения через `App.run_test()` (Textual Pilot) и экспорт SVG-снимков. Снято **30+ состояний** на размерах от **40×16** до **200×32**: приветствие, чат, генерация, справка, темы, модели, подтверждение инструмента, визард вопросов, diff, узкий/широкий терминал, светлые темы.
3. **Пиксельные замеры** — SVG-снимки растеризуются обратно в символьную сетку с сохранением **fg/bg каждого знакоместа**, после чего считается контраст по WCAG 2.1 для:
   - всех пар токенов всех 21 темы (13 пар на тему);
   - фактически отрендеренных текстовых ячеек и ячеек границ/разделителей (в замере спринта 1 — 5 279 текстовых и 1 432 ячейки границ на 17–18 сценариях без модалок; текст под затемнением модалки не учитывается, он приглушён намеренно).
4. **Чек-лист** современного TUI/CLI-ассистента (Claude Code, Codex CLI, Crush, OpenCode, Amp как референсная группа) + WCAG 2.1 AA как измеримая часть доступности.

Снимки — в этой же папке (`*.png`), скрипты прогона — в `scripts/` (в `.gitignore`, в поставку не входят; по запросу закоммичу).

---

## 2. Итоговая оценка

| Категория | Оценка | Кратко |
|---|---|---|
| Дизайн-система и темы | **9.0** | 21 тема с полными наборами токенов, 0 хардкодных hex в CSS |
| Информационная архитектура / плотность | **8.0** | Двухстрочный футер несёт 11 метрик, иерархия чата читаемая |
| Клавиатурный UX и discoverability | **7.5 → 8.0** | Единый Esc, справка, редкие алиасы раскладок, единая формулировка esc и объявленный `space` (спринт 3); но нет палитры команд |
| Адаптивность (responsive) | **6.5 → 8.5 → 9.0** | 3 брейкпоинта реализованы; футер больше не теряет данные на 75–99 колонках (спринт 1); проза ограничена 110 знакоместами на широких терминалах (спринт 3) |
| Обратная связь и состояния | **6.0 → 8.0 → 8.5** | Спиннер и контекст-метр есть; после спринта 2 — «esc: interrupt • 12.4s», пустые состояния, скроллбар и счётчик в списках; после спринта 3 — обученное приветствие |
| Доступность (контраст, фокус, выделение) | **5.5 → 8.0** | После спринта 1: 3.1% текста ниже AA, границы ≥3:1, видимый маркер выделения; мышь по-прежнему не выделяет текст |
| Гигиена кода стилей | **7.0 → 7.5 → 8.5** | Всё на токенах; мертвый токен и хардкод статусных цветов убраны в спринте 2; `!important` — 41 → 6 в спринте 3 |

**Итог: 7.1 / 10 (B)** на момент аудита; **после спринта 1 — 7.7 / 10 (B+)**; **после спринта 2 — 8.1 / 10 (B+)**; **после спринта 3 — 8.4 / 10 (A-)** — зрелая, «взрослая» TUI с сильной дизайн-системой. Спринт 1 (P0-1…P0-4) закрыл системную недоработку контраста и баг вёрстки футера на массовом диапазоне 80–100 колонок; спринт 2 (P1-5/6/8/9) — обратную связь, работу с мышью и статусные цвета; спринт 3 (P1-7, P2-10…P2-14) — читаемость на широких терминалах, обучение на пустом экране, консистентность подсказок и ревизию `!important`.

Что осталось открытым (не P0/P1): нет слушателя смены темы, поэтому уже отрисованные карточки инструментов сохраняют старый цвет точки до перерисовки; палитра команд по-прежнему отключена; нет явного флага reduced-motion; описание команды в popup подсказок — `[dim]` 3.44:1 (единственный текст ниже AA, ~1% ячеек).

---

## 3. Сильные стороны (что уже на уровне лучших практик)

**1. Дизайн-система уровня продукта, а не скрипта.** 21 тема, каждая задана не «пятью цветами», а тремя слоями: `tcss_vars` (13 токенов), `markdown_styles` (~25 правил markdown), `syntax_tokens` (25 pygments-токенов). Проверено рендером: светлые темы (`github-light`, `zinc-light`) полностью перекрашивают UI — фон `#ffffff`, поверхность `#f6f8fa`, подсветка кода берётся из GitHub-палитры (`#cf222e`, `#8250df`, `#953800`), а не из тёмной. В `app.tcss` — **ни одного литерального hex**: всё через `$токен`. Это редкий уровень дисциплины.

**2. Адаптивность реализована как система, а не как набор костылей.** `widgets/utils/responsive.py` — единый источник брейкпоинтов (`BREAKPOINT_BANNER=52`, `BREAKPOINT_HINT=60`, `BREAKPOINT_COMPACT=75`) с запретом инлайнить числа в виджетах; модалки подстраивают ширину под контент (`fit_modal_width`, коридор 44–104, не более 90% терминала). Проверено: на 40 колонках ASCII-баннер заменяется вордмарком `johnston`, футер переходит в компактный режим и корректно обрезает значения многоточием — `johnston • arena/01a05e32-j... • review`.

**3. Клавиатурная модель последовательна.** Esc закрывает любой экран, `ctrl+c`/`ctrl+q` — выход, везде. Справка (`/help`) — две вкладки: 18 команд и 18 биндингов, и они **соответствуют реальным `BINDINGS`**. Отдельно: `widgets/utils/key_aliases.py` разворачивает хоткеи по раскладкам RU/UA/BY/SR/EL/HE/AR/KA/HY — Ctrl+C работает при включённой кириллице. Этого нет в большинстве конкурентов.

**4. Диалог подтверждения инструментов — образцовый.** `PermissionConfirmScreen` показывает дифф, цель, агента и 5 явных вариантов (`Allow once` / `Allow pattern (widgets/**)` / `Always allow for session` / `Deny` / `Reject with feedback...`), плюс хоткеи `enter/p/a/r/d` и подсказку `enter: select • r: feedback • esc: deny` — т.е. вариант по умолчанию безопасен и назван словами, а не цветом.

**5. Плотность информации без визуального шума.** Футер вмещает роль, провайдер, модель, контекст-метр, токены, стоимость, каталог, ветку, дифф, sandbox и режим в две строки; метрики при этом не спорят с чатом (монохром + один акцент на метре).

**6. Markdown-рендер продуктного уровня:** таблицы, списки, код с подсветкой, языком и кнопкой `copy`, врезки, задачи. Ширина строк считается через `cell_len`/`display_width` — CJK и эмодзи не ломают вёрстку.

---

## 4. Найденные проблемы

### P0 — критично (массовый охват или ломает сценарий)

**P0-1. Контраст: 30% текста ниже WCAG AA.** ✅ *исправлено в спринте 1*
Замер по 5 279 отрендеренным текстовым ячейкам (сценарии без модалок): **39.1% ниже 4.5:1**; по границам/разделителям — **82.8% ниже 3:1** (WCAG 1.4.11).
По токенам: `fg-muted` (им пользуются thinking-блоки, плейсхолдер ввода, подписи, bullets, логотип) не дотягивает до 4.5:1 в **18 из 20** тем: в теме по умолчанию `zinc` — **3.67:1** на фоне приложения и **3.08:1** на поверхности (это футер, подпись `python`, кнопка `copy`). Границы панелей — 1.2–1.7:1 во всех темах.
*Что сделать:* поднять `muted` до ≥4.5:1 (в zinc: `#71717a` → ~`#8a8a93`), `border` — до ≥3:1 для границ, несущих смысл (модалки, панели кода); ввести отдельный `fg-disabled` для декоративных элементов (ASCII-логотип), к которым AA не применяется. Добавить в CI проверку контраста токенов — это 30 строк и она навсегда закроет класс проблем.

**P0-2. Футер теряет данные при 75–99 колонках.** ✅ *исправлено в спринте 1*
`StatusFooter._apply_two_row_grid` (widgets/status_footer.py:113) собирает Rich-таблицу `Table.grid(expand=True)` с двумя колонками: при `expand=True` Rich делит ширину **поровну**, левая ячейка получает ~половину, вторая строка переносится, а высота виджета фиксирована (2 строки) — хвост **молча обрезается, без многоточия**. Компактная вёрстка включается только ниже 75 колонок.
Замерено на реальном приложении:

| Ширина | Строка 2 футера |
|---|---|
| 70 | `johnston • arena/01a05e32-john... • review` ✅ (компактный режим, корректное многоточие) |
| **80** | `/home/user/johnston  •` ❌ — потеряны ветка и режим, висит разделитель |
| **90** | `/home/user/johnston  •  arena/01a05e32-johnston` ❌ — потерян режим |
| 100+ | полная строка ✅ |

Диапазон 80–100 — это дефолтный терминал и любой вертикальный сплит, т.е. самый массовый кейс.
*Что сделать:* в широкой ветке (status_footer.py:294) собирать строку так же, как в компактной — с бюджетом по `width` и `ellipsize()` у каждого сегмента, а не надеяться на клип таблицы; либо задать первой колонке `ratio`. Регрессионный тест на 80/90 колонок обязателен.

**P0-3. Модальный оверлей: токен не существует → светлые темы накрывает серым.** ✅ *исправлено в спринте 1*
`app.tcss:531` задаёт `ModalScreen { background: $bg-overlay; }`, но `$bg-overlay` **не определён ни в одной теме** (`themes.json`, `theme_manager.py` — токена нет). Правило мертво, применяется дефолт Textual — `rgba(0,0,0,0.45)`, **всегда чёрный**, независимо от темы. Замерено: в `github-light` фон за модалкой становится `#8c8c8c` (был `#ffffff`), отделение диалога от подложки — **3.16:1**; в тёмной теме диалог/подложка — **1.30:1**, т.е. границы модалки почти не читаются, а затемнённый чат под ней остаётся читаемым (**4.73:1**) и отвлекает.
*Что сделать:* добавить `bg-overlay` в каждую тему (тёмные: `rgba(0,0,0,0.6)`, светлые: `rgba(255,255,255,0.6)` или затемнение в тон), после чего перепроверить отделение диалога от фона (цель ≥1.5–2:1 по яркости плюс `border` ≥3:1).

**P0-4. Индикация фокуса в модалках — только жирный шрифт.** ✅ *исправлено в спринте 1*
`app.tcss:1045` — для `#modal-dialog OptionList > .option-list--option-highlighted` задано `background: transparent; color: $fg-primary; text-style: bold;`, а hover тоже `transparent`. То есть единственный признак выбранной строки — **bold**. Проверено растеризацией SVG: после 3× `down` выделенная строка `Tokyo Night` ничем, кроме `font-weight`, не отличается. Глобальный стиль (`app.tcss:387`) использует инверсию `$bg-inverted/$fg-inverted` — но он перекрыт.
*Что сделать:* вернуть видимый маркер — инверсия строки или символ-курсор (`▸`) слева; это же решит проблему «где я» при прокрутке длинного списка.

### P1 — важно

**P1-5. Статусные точки инструментов выпадают из системы тем.** ✅ *исправлено в спринте 2* `core/domain/defaults/config.py:89-91` — `RUNNING #d4a259`, `ERROR #d15858`, `SUCCESS #5ea876` — константы, подобранные под тёмный zinc, и используются в обход `$accent-*`. На светлых темах: **2.86:1** (success на белом) и **2.30:1** (running). Плюс 21 тема со своими `accent-success/error/warning` в этом месте просто не работает.
*Что сделать:* брать цвета из активной темы (`theme_manager.current_theme.tcss_vars['accent-*']`) с фолбэком на константы.

**P1-6. Скроллбары отключены глобально, индикаторов позиции нет.** ✅ *исправлено в спринте 2* `app.tcss:36` — `* { scrollbar-size: 0 0 !important; }` (ещё 8 аналогичных правил). Последствия measurable: список тем — 21 пункт, показывается **12** (`app.tcss:1007` `max-height: 12`) без намёка на остальное; меню slash-команд показывает **3 строки из 57** вариантов (`app.tcss:406` `max-height: 5`) — пользователь не знает, что есть `/rewind`, `/fork`, `/diff`, пока не начнёт печатать. В чате — ни полосы, ни счётчика, ни «⬇ новые сообщения».
*Что сделать:* вернуть тонкий скроллбар хотя бы в списковые экраны, добавить «N/M» или «ещё ↓» в подсказку; для popup поднять `max-height` до 8–10 и добавить индикатор.

**P1-7. Нет ограничения ширины текста на широких терминалах.** ✅ *исправлено в спринте 3* На 200 колонках абзац рендерится строкой **~180 символов** (снимок `21-wide-200.png`) — это заведомо за пределами комфортной длины строки (рекомендуемая 60–100, жёсткий предел ~120). Референсные TUI и веб-читалки режут колонку.
*Что сделать:* `max-width: 110` (или `min(100%, 110)`) для контента сообщений, код оставить во всю ширину.

**P1-8. Выделение мышью отключено почти везде.** ✅ *исправлено в спринте 2* `ALLOW_SELECT = False` стоит на 22 классах: сообщения чата, markdown, вывод инструментов, diff, футер, модалки (при дефолте Textual — `True`). Мышью можно выделить только ввод и popup подсказок; единственный путь скопировать ответ — кнопка `copy` у блока кода или `/copy`.
*Что сделать:* включить `ALLOW_SELECT` хотя бы для тела сообщений и вывода инструментов — это базовое ожидание от терминала сегодня.

**P1-9. Нет обратной связи во время генерации.** ✅ *исправлено в спринте 2* На снимке `20-generating.png`: спиннер `⠸`, метр `64%`, токены, цена — но **нет ни «esc — прервать», ни таймера, ни индикатора стрима**; плейсхолдер ввода не меняется. Прерывание есть (документировано в `/help`), но не показано в момент, когда оно нужно. Пустые списки тоже молчаливы: `/subagents` и `/shell` без задач не открывают экран, а лишь вызывают `notify` (тост в headless не рендерится, но в коде ветка есть).
*Что сделать:* на время генерации показывать в футер/нотче `esc: interrupt • 12.4s`; для пустых списков — открывать экран с пустым состоянием и подсказкой, что делать.

### P2 — полировка

**P2-10. Пустой экран приветствия.** ✅ *исправлено в спринте 3* Только ASCII-логотип: нет версии, подсказок (`/help`, `@file`, `Ctrl+O`), быстрых действий, состояния подключения. Первый запуск — единственный шанс обучить, он не используется.
**P2-11. Несогласованность подсказок.** ✅ *исправлено в спринте 3* Один и тот же Esc — `esc: cancel` (темы, модели) и `esc: close` (справка, diff, skills). В `/skills` подсказка обещает `space: toggle`, но в `BINDINGS` `space` не объявлен (обработчик есть — `skills.py:167`): биндинг работает, но невидим для футера/справки.
**P2-12. Мелочи markdown-рендера.** ✅ *исправлено в спринте 3* Список задач рендерится как `• [x] …` (буллит дублирует чекбокс); цитата — без вертикальной черты, только отступ; имена моделей «очеловечиваются» до `Claude Sonnet 4 5` и `GPT 4o` — теряется исходный идентификатор, по которому модель ищется.
**P2-13. Diff: в сайдбаре только basename** ✅ *исправлено в спринте 3* (`status_footer.py`) без пути и без многоточия — при одноимённых файлах список неоднозначен.
**P2-14. `!important` × 41 в `app.tcss`** ✅ *исправлено в спринте 3* — в основном ради обнулённого скроллбара и прозрачных фокусов. Каждое такое правило — это блокировка будущих правок тем.

---

## 5. Чек-лист «современный TUI-ассистент»

| Требование | Статус | Комментарий |
|---|---|---|
| Единая дизайн-система, токены вместо литералов | ✅ | 21 тема × 3 слоя токенов, 0 hex в CSS |
| Светлая и тёмная темы | ✅ | 4 светлые темы рендерятся полностью; оверлей из темы (P0-3), статусные точки с контраст-коррекцией (P1-5) |
| Адаптивность под узкие терминалы | ✅ | 40…200 колонок; потеря данных на 75–99 устранена (P0-2) |
| Контроль длины строки на широких экранах | ✅ | проза ≤110 знакомест, код во всю ширину (P1-7) |
| Контраст WCAG AA (4.5:1 текст, 3:1 UI) | ⚠️ | 39.1% → 3.1% текста ниже AA и 82.8% → 0% границ (P0-1); вне замеров остаётся `[dim]` в описании команды popup — 3.44:1 |
| Видимый фокус/выделение | ✅ | Инверсия + bold в модалках (P0-4) |
| Информация не только цветом | ✅ | Статусы — `●` + текст, diff — `+/-`, опции — словами |
| Keyboard-first + discoverability | ✅ | Esc/Enter единообразно, `/help` полный, алиасы раскладок |
| Палитра команд / автодополнение | ⚠️ | Встроенная палитра отключена; `/`-меню показывает 3 из 57 (P1-6) |
| Состояния загрузки/стрима | ✅ | Спиннер, метр, `esc: interrupt • 12.4s` и таймер генерации (P1-9) |
| Пустые состояния | ✅ | Списки — состояние с подсказкой (P1-9), приветствие — версия, подключение и подсказки (P2-10) |
| Ошибки, различимые и объяснённые | ✅ | Тосты по severity,转义 статусы инструментов, returncode |
| Выделение/копирование мышью | ✅ | Вывод инструментов и содержимое diff выделяются; хром — нет, чтобы клик по файлу не был выделением (P1-8) |
| Индикаторы прокрутки | ✅ | Скроллбар и счётчик в списках, проза и чат без баров (P1-6) |
| i18n / не-латинские раскладки | ✅ | 8 алфавитов в алиасах клавиш, `cell_len` для CJK |
| Ограничение анимаций (reduced motion) | ➖ | Анимации почти везде отключены (`animate=False`), но явного флага нет — сейчас не проблема |
| Доступность для скринридеров | ➖ | Для TUI определяется терминалом; в web-режиме (`textual serve`) ARIA-разметки нет |

---

## 6. Приоритетный план

**Спринт 1 (закроет ~70% претензий к доступности и главный баг вёрстки)**
1. P0-2 — бюджет + `ellipsize` в широкой ветке футера + регресс-тест на 80/90/100 колонок.
2. P0-1 — поднять `muted`/`border` во всех темах, добавить CI-проверку контраста (fail ниже 4.5 / 3.0).
3. P0-3 — ввести `bg-overlay` в темы.
4. P0-4 — видимый маркер выделения в модалках.

**Спринт 2 — выполнено**
5. P1-5 — статусные цвета из темы (+ контраст-коррекция до 4.5:1). ✅
6. P1-6 — скроллбар/счётчик в списках, `max-height` popup 5 → 9. ✅
7. P1-8 — `ALLOW_SELECT` для вывода инструментов и содержимого diff. ✅
8. P1-9 — `esc: interrupt` + таймер генерации; пустые состояния вместо тостов. ✅

**Спринт 3 — выполнено**
9. P1-7 — `max-width` 110 для связного текста, код во всю ширину. ✅
10. P2-10 — приветствие: версия, состояние подключения, блок подсказок. ✅
11. P2-11 — три формулировки esc (`close`/`cancel`/`back`) + объявленный биндинг `space`. ✅
12. P2-12 — чекбоксы вместо `• [x]`, черта у цитаты, id модели в `/models`. ✅
13. P2-13 — путь от cwd в сайдбаре diff, обрезка по середине. ✅
14. P2-14 — `!important` 41 → 6, выключатель скроллбаров на `Widget`. ✅

**Дальше (не в этой работе):** слушатель смены темы для статусных точек, палитра команд,
флаг reduced-motion, `[dim]` в описании команды popup (3.44:1).

---

## 7. Снимки

| Файл | Что показывает |
|---|---|
| `01-welcome-140.png` | Приветствие: только логотип, нет версии и подсказок (P2-10) |
| `02-chat-140.png` | Основной чат: код, таблица, цитата, вызовы инструментов, футер |
| `02-chat-140-after.png` | Тот же чат после спринта 1: границы ≥3:1, `muted` ≥4.5:1 |
| `11-chat-80-after.png` | 80 колонок после спринта 1: ветка и режим в футере сохранены |
| `27-modal-highlight-after.png` | Выделенная строка в модалке: инверсия вместо одного bold |
| `30-github-light-after.png` | Светлая тема: оверлей из `bg-overlay`, а не чёрный по умолчанию |
| `03-help-commands.png` | Справка: 18 команд (вкладка 2 — биндинги) |
| `05-theme.png` | 21 тема, видно 12, без индикатора прокрутки (P1-6, P0-4) |
| `06-models.png` | Выбор модели: группировка, теги `vision` |
| `07-permission.png` | Подтверждение инструмента: дифф + 5 явных вариантов |
| `08-askuser.png` | Визард вопросов с описаниями опций |
| `09-diff.png` | Полноэкранный diff: сайдбар, хидер, подсказки |
| `11-chat-80.png` | 80 колонок: футер обрезан — `/home/user/johnston  •` (P0-2) |
| `20-generating.png` | Состояние генерации: спиннер и метр без «esc — прервать» (P1-9) |
| `22-narrow-40x16.png` | 40 колонок: вордмарк вместо баннера, компактный футер ✅ |
| `28-slash-popup.png` | `/`-меню: 3 строки из 57 кандидатов (P1-6) |
| `30-github-light.png` | Светлая тема: рендер корректен, но точки инструментов тёмные (P1-5) |
| `40-generating-footer.png` | Спринт 2: `esc: interrupt • 1.4s` в футере |
| `41-subagents-empty.png` | Спринт 2: пустое состояние вместо тоста |
| `42-welcome-screen.png` | Спринт 3: версия, состояние подключения, блок подсказок |
| `43-prose-max-width.png` | Спринт 3: 200 колонок — проза 110, код во всю ширину |

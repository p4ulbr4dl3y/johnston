"""Application orchestrators — business logic layered under core.

Subpackages: generation (AI response + prompt building), session (session
actions), provider (provider/model/thinking-effort ops).
"""
